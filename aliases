alias be='bundle exec'
alias g=git
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='../..'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'

# Include custom aliases
[[ -f ~/.aliases.local ]] && source ~/.aliases.local
