alias vi=vim
alias be='bundle exec'
alias g=git
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='cd ../..'
alias l='ls'
alias ll='ls -Alh'
alias tmux='tmux -u'

# Include custom aliases
[[ -f ~/.aliases.local ]] && source ~/.aliases.local

