alias vi=nvim
alias vim=nvim
alias g=git
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='cd ../..'
alias ls='ls --color=auto'
alias l='ls'
alias ll='ls -Alh'

# Include custom aliases
[[ -f ~/.aliases.local.sh ]] && source ~/.aliases.local.sh
